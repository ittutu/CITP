lang CITP

`` -----------------------------------------------------------------------------

import Maude module FG

goal INF is
|-{FG}
  fm forall {X:Nat} s^9(0) <= G(F(X:Nat)) + G(X:Nat) = true [metadata "geq9"] .
endg

begin proof P-INF of INF
  tc ca red
  show metadata
  pull("1") pull("2") trans red
qed

`` -----------------------------------------------------------------------------

import Maude module LEQ

goal TOTAL is
|-{LEQ}
  fm forall {X:Nat, Y:Nat} X:Nat <= Y:Nat = true \/ Y:Nat <= X:Nat = true
     [metadata "total"] .
endg

begin proof P-TOTAL of TOTAL
  ind(X:Nat, Y:Nat) red
  init("total/IH[x#1]", Y:Nat <- x#2) red
qed

`` -----------------------------------------------------------------------------
